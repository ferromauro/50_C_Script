/* Primo script da compilare via linea di comando.
 * Su Linux utilizzare il comando gcc per la compilazione del programma.
 * 
 * ESEMPIO: gcc script_01.c -o nome_file_eseguibile 
 * (Nella cartella 'compiled' avete tutti gli script compilati)
 *  */
#include <stdio.h>

int main()
{
	printf("Hello, World! This is a native C program compiled on the command line.\n");
	return 0;
}
