// Programma per leggere l'input dell'utente e farne la somma.
#include <stdio.h>
int main(void){
	int A;
	int B;
	printf("Inserisci il valore di A:");
	scanf("%d",&A);
	printf("Inserisci il valore di B:");
	scanf("%d",&B);
	printf("La somma di A e B è pari ad:%d\n",A+B);
}

// NOTA: se non vengono inseriti degli interi il programma non funziona correttamente.
